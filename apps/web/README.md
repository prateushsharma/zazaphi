# @zazaphi/web

The product UI: describe an app, watch the build stream, inspect the preview,
approve the production deploy, and read the token/cost dashboard.

Built on the `@zazaphi/api` backend. Implemented after the engine subsystems
are complete.
